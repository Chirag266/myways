First install express.
Then install nodemon.
Then open backend file by typing nodemon broadcast.js.
Open the localhost:3001 on normal tab.
open localhost:3001 in incognito tab as well.
u can see a connection is established and two user can send messages to each other but once one message is send before message will delete itself.